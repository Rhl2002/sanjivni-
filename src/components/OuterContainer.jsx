import styled from "styled-components";

const OuterContainer = styled.div`
  border: 30px solid #676a81; /* Adjust width, color, and style as needed */
  padding: 10px; /* Optional padding to avoid content overlapping the border */
`;

export default OuterContainer;
