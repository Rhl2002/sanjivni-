import styled from "styled-components";

const InnerContainer = styled.div`
  border: 30px solid #5a5c70; /* Adjust width, color, and style as needed */
  padding: 10px; /* Optional padding to avoid content overlapping the border */
  margin: -15px;
`;

export default InnerContainer;
